# включить  " iostream "
# включить  " cmath "
использование  пространства имен  std ;
int  main ()
{
	double x, y, z;
	const  double pi = 3,1415 ;
	cin >> x >> y >> z;
	double a, b;
	а = ( 2 * соз (х + (пи / 4 ))) / ( 1 / 2 + ( грех (у) * грех (у))) + ( экоса (х + (р / 2 )));
	b = ( 1 ) + (z * z) / ( 3 + (z * z / 2 ));
	cout << a << endl << b;
	система ( « пауза » );
    return  0 ;
}